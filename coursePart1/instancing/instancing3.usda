#usda 1.0
(
	 subLayers = [@./instancing1.usda@]
	 doc = """ 
	 This shows that when we switch instanceable off on TreeB via a sublayer, it gets it's .666 override back that was previously thrown away"""
)

over "TreeB" (
	instanceable = false
	doc = " we get our .666 override back as we've switched off instancing!"
	)
	{}