#usda 1.0
(
	doc = "We reference some stuff which has references at A and APrime"
    defaultPrim = "APrimePrime"
)
def "APrimePrime" (
)
{
	def "APrime" (
	add references = [ @level3.usda@ ]
	) 
	{}
}